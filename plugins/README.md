# Plugins

Project-local plugins and experiments belong here.

Use one subdirectory per plugin. Each plugin should include its own README that
explains:

- what it integrates with,
- how it is built or installed,
- which generated files are safe to ignore.

Keep generated outputs out of Git unless they are required source artifacts.
